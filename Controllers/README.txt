Here is where all major computation and logic should be since
most of the models are just dumb objects, and renderers only know how to render
themselves, they have the models needed for it obviously, but still don't have a 
lot of logic to think about what's given to them
